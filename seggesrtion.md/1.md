
# Now let me generate the actual frontend code files
# I'll create a complete Next.js 16 App Router frontend with all the pages

# First, let's create the directory structure and files

import os

base_dir = "/mnt/agents/output/opsora-web"
os.makedirs(base_dir, exist_ok=True)

# Create the project structure
structure = [
    "src/app/(auth)/login/page.tsx",
    "src/app/(auth)/layout.tsx",
    "src/app/(dashboard)/page.tsx",
    "src/app/(dashboard)/layout.tsx",
    "src/app/(dashboard)/residents/page.tsx",
    "src/app/(dashboard)/residents/new/page.tsx",
    "src/app/(dashboard)/payments/page.tsx",
    "src/app/(dashboard)/payments/verify/page.tsx",
    "src/app/(dashboard)/rooms/page.tsx",
    "src/app/(dashboard)/complaints/page.tsx",
    "src/app/(dashboard)/iot/page.tsx",
    "src/app/(dashboard)/food/page.tsx",
    "src/app/(dashboard)/staff/page.tsx",
    "src/app/tenant/page.tsx",
    "src/app/tenant/layout.tsx",
    "src/components/ui/button.tsx",
    "src/components/ui/card.tsx",
    "src/components/ui/modal.tsx",
    "src/components/ui/toast.tsx",
    "src/components/layout/sidebar.tsx",
    "src/components/layout/mobile-nav.tsx",
    "src/components/dashboard/revenue-card.tsx",
    "src/components/dashboard/occupancy-card.tsx",
    "src/components/dashboard/complaint-card.tsx",
    "src/components/modals/checkout-warning.tsx",
    "src/components/modals/payment-verify-warning.tsx",
    "src/components/modals/bulk-invoice-warning.tsx",
    "src/components/modals/delete-warning.tsx",
    "src/lib/api.ts",
    "src/lib/auth.ts",
    "src/middleware.ts",
    "src/hooks/use-auth.ts",
    "src/hooks/use-toast.ts",
    "package.json",
    "tailwind.config.ts",
    "next.config.js",
]

for path in structure:
    full_path = os.path.join(base_dir, path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    
print("Directory structure created!")
print(f"Total files to generate: {len(structure)}")



fiel 2:


# Generate the core files - let's start with the most critical ones

files = {}

# 1. package.json
files["package.json"] = '''{
  "name": "opsora-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0",
    "lucide-react": "^0.460.0",
    "@phosphor-icons/react": "^2.1.7",
    "recharts": "^2.13.0",
    "swr": "^2.2.5",
    "zustand": "^5.0.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.0",
    "date-fns": "^4.1.0",
    "js-cookie": "^3.0.5",
    "@types/js-cookie": "^3.0.6"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "postcss": "^8.4.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "16.0.0"
  }
}
'''

# 2. tailwind.config.ts
files["tailwind.config.ts"] = '''import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        saffron: {
          50: '#FFF8F0',
          100: '#FFE8D0',
          200: '#FFD0A0',
          300: '#FFB870',
          400: '#FFA040',
          500: '#FF9933',
          600: '#E67A00',
          700: '#B35F00',
        },
        'deep-green': {
          50: '#F0FFF4',
          100: '#D1FADF',
          500: '#138808',
          600: '#0E6606',
        },
        'warm-cream': '#FFF8F0',
        charcoal: '#2D2D2D',
        'alert-red': '#DC2626',
        'pending-amber': '#D97706',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        display: ['Playfair Display', 'Georgia', 'serif'],
      },
      animation: {
        'subtle-pulse': 'subtle-pulse 2s ease-in-out infinite',
        'shimmer': 'shimmer 1.5s infinite',
      },
      keyframes: {
        'subtle-pulse': {
          '0%, 100%': { opacity: '1' },
          '50%': { opacity: '0.92' },
        },
        shimmer: {
          '0%': { backgroundPosition: '-200% 0' },
          '100%': { backgroundPosition: '200% 0' },
        },
      },
    },
  },
  plugins: [],
};

export default config;
'''

# 3. next.config.js
files["next.config.js"] = '''/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'http://localhost:3001/:path*',
      },
    ];
  },
};

module.exports = nextConfig;
'''

# 4. src/lib/api.ts
files["src/lib/api.ts"] = '''"use client";

import Cookies from 'js-cookie';

const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001';

export interface ApiError {
  error: string;
  details?: Array<{ path: string; message: string; code: string }>;
}

export async function apiFetch<T>(
  endpoint: string,
  options: RequestInit = {}
): Promise<T> {
  const token = Cookies.get('opsora_token');
  
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(token ? { Authorization: `Bearer ${token}` } : {}),
    ...((options.headers as Record<string, string>) || {}),
  };

  const response = await fetch(`${API_BASE}${endpoint}`, {
    ...options,
    headers,
  });

  const data = await response.json().catch(() => null);

  if (!response.ok) {
    const error = new Error(data?.error || `HTTP ${response.status}`);
    (error as any).status = response.status;
    (error as any).data = data;
    throw error;
  }

  return data as T;
}

// Auth APIs
export const authApi = {
  login: (email: string, password: string) =>
    apiFetch<{ token: string; user: { id: string; email: string; role: string; fullName: string; tenantId: string } }>('/auth/login', {
      method: 'POST',
      body: JSON.stringify({ email, password }),
    }),
  
  register: (data: { name: string; slug: string; email: string; phone: string; password: string; address?: string; city?: string; state?: string; pincode?: string }) =>
    apiFetch<{ token: string; tenant: { id: string; name: string; slug: string }; user: { id: string; email: string; role: string } }>('/auth/register', {
      method: 'POST',
      body: JSON.stringify(data),
    }),
  
  me: () =>
    apiFetch<{ id: string; email: string; fullName: string; role: string; tenantId: string }>('/auth/me'),
  
  logout: () =>
    apiFetch('/auth/logout', { method: 'POST' }),
};

// Dashboard APIs
export const dashboardApi = {
  overview: () =>
    apiFetch<{
      properties: { total: number; totalBeds: number; occupiedBeds: number; vacantBeds: number; occupancyRate: string };
      tenants: { active: number };
      payments: { totalExpected: number; totalCollected: number; totalPending: number; collectionRate: string; paidCount: number; pendingCount: number };
      complaints: { open: number; urgent: number };
      water: Array<{ tankId: string; tankName: string; capacityLiters: number; currentLevel: number | null }>;
      visitors: { pending: number };
      recentActivity: Array<any>;
    }>('/dashboard/overview'),
};

// Resident APIs
export const residentApi = {
  list: (params?: { page?: number; limit?: number; search?: string; status?: string }) =>
    apiFetch<{ data: Array<any>; pagination: { page: number; limit: number; total: number; totalPages: number } }>(`/residents?${new URLSearchParams(params as any)}`),
  
  create: (data: any) =>
    apiFetch('/residents', { method: 'POST', body: JSON.stringify(data) }),
  
  get: (id: string) =>
    apiFetch(`/residents/${id}`),
  
  checkout: (id: string) =>
    apiFetch(`/residents/${id}/checkout`, { method: 'POST' }),
};

// Payment APIs
export const paymentApi = {
  list: (params?: { page?: number; limit?: number; status?: string; monthYear?: string }) =>
    apiFetch(`/payments?${new URLSearchParams(params as any)}`),
  
  summary: () =>
    apiFetch('/payments/summary'),
  
  create: (data: any) =>
    apiFetch('/payments', { method: 'POST', body: JSON.stringify(data) }),
  
  pay: (id: string, data: { paidAmount: number; paymentMethod: string }) =>
    apiFetch(`/payments/${id}/pay`, { method: 'POST', body: JSON.stringify(data) }),
};

// Payment Proof APIs
export const paymentProofApi = {
  invoices: (params?: { page?: number; limit?: number; status?: string }) =>
    apiFetch(`/payments-proof/invoices?${new URLSearchParams(params as any)}`),
  
  generateInvoices: (data: { monthYear: string; dueDate: string }) =>
    apiFetch('/payments-proof/invoices/generate', { method: 'POST', body: JSON.stringify(data) }),
  
  proofs: (params?: { page?: number; limit?: number; status?: string }) =>
    apiFetch(`/payments-proof/proofs?${new URLSearchParams(params as any)}`),
  
  verifyProof: (id: string, data: { verificationStatus: string; rejectionReason?: string; notes?: string }) =>
    apiFetch(`/payments-proof/proofs/${id}/verify`, { method: 'POST', body: JSON.stringify(data) }),
  
  dashboard: () =>
    apiFetch('/payments-proof/dashboard'),
};

// Complaint APIs
export const complaintApi = {
  list: (params?: { page?: number; limit?: number; status?: string; priority?: string }) =>
    apiFetch(`/complaints?${new URLSearchParams(params as any)}`),
  
  create: (data: any) =>
    apiFetch('/complaints', { method: 'POST', body: JSON.stringify(data) }),
  
  updateStatus: (id: string, data: { status: string; assignedTo?: string; resolutionNotes?: string }) =>
    apiFetch(`/complaints/${id}/status`, { method: 'PATCH', body: JSON.stringify(data) }),
};

// Staff APIs
export const staffApi = {
  dashboard: () =>
    apiFetch('/staff/dashboard'),
  
  tickets: (params?: any) =>
    apiFetch(`/staff/tickets?${new URLSearchParams(params as any)}`),
  
  checkin: () =>
    apiFetch('/staff/checkin', { method: 'POST' }),
  
  checkout: () =>
    apiFetch('/staff/checkout', { method: 'POST' }),
};
'''

# 5. src/lib/auth.ts
files["src/lib/auth.ts"] = '''"use client";

import Cookies from 'js-cookie';

export interface User {
  id: string;
  email: string;
  fullName: string;
  role: 'owner' | 'admin' | 'staff' | 'resident';
  tenantId: string;
}

export function setAuth(token: string, user: User) {
  Cookies.set('opsora_token', token, { expires: 1, secure: false, sameSite: 'lax' });
  Cookies.set('opsora_role', user.role, { expires: 1, secure: false, sameSite: 'lax' });
  Cookies.set('opsora_user', JSON.stringify(user), { expires: 1, secure: false, sameSite: 'lax' });
}

export function clearAuth() {
  Cookies.remove('opsora_token');
  Cookies.remove('opsora_role');
  Cookies.remove('opsora_user');
}

export function getToken(): string | undefined {
  return Cookies.get('opsora_token');
}

export function getUser(): User | null {
  const userStr = Cookies.get('opsora_user');
  if (!userStr) return null;
  try {
    return JSON.parse(userStr);
  } catch {
    return null;
  }
}

export function getRole(): string | undefined {
  return Cookies.get('opsora_role');
}

export function isAuthenticated(): boolean {
  return !!getToken();
}

export function hasRole(roles: string[]): boolean {
  const role = getRole();
  return role ? roles.includes(role) : false;
}
'''

# 6. src/middleware.ts
files["src/middleware.ts"] = '''import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

const PUBLIC_PATHS = ['/login', '/register', '/_next', '/api'];
const OWNER_PATHS = ['/payments', '/rooms', '/iot', '/food', '/users'];
const STAFF_PATHS = ['/staff'];
const RESIDENT_PATHS = ['/tenant'];

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;
  
  // Allow public paths
  if (PUBLIC_PATHS.some(p => pathname.startsWith(p))) {
    return NextResponse.next();
  }
  
  const token = request.cookies.get('opsora_token')?.value;
  const role = request.cookies.get('opsora_role')?.value;
  
  // Not authenticated
  if (!token) {
    return NextResponse.redirect(new URL('/login', request.url));
  }
  
  // Role-based access
  if (pathname.startsWith('/tenant') && role !== 'resident') {
    return NextResponse.redirect(new URL('/dashboard', request.url));
  }
  
  if ((pathname.startsWith('/staff') || pathname.startsWith('/payments')) && 
      role === 'resident') {
    return NextResponse.redirect(new URL('/tenant', request.url));
  }
  
  return NextResponse.next();
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.png$).*)'],
};
'''

# 7. src/hooks/use-auth.ts
files["src/hooks/use-auth.ts"] = '''"use client";

import { useState, useEffect } from 'react';
import { getUser, getRole, isAuthenticated, clearAuth, User } from '@/lib/auth';

export function useAuth() {
  const [user, setUser] = useState<User | null>(null);
  const [role, setRole] = useState<string | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    setUser(getUser());
    setRole(getRole() || null);
    setLoading(false);
  }, []);

  return {
    user,
    role,
    isAuthenticated: isAuthenticated(),
    isOwner: role === 'owner',
    isAdmin: role === 'admin',
    isStaff: role === 'staff',
    isResident: role === 'resident',
    loading,
    logout: () => {
      clearAuth();
      window.location.href = '/login';
    },
  };
}
'''

# 8. src/hooks/use-toast.ts
files["src/hooks/use-toast.ts"] = '''"use client";

import { useState, useCallback } from 'react';

export interface Toast {
  id: string;
  message: string;
  type: 'success' | 'error' | 'warning' | 'info';
}

export function useToast() {
  const [toasts, setToasts] = useState<Toast[]>([]);

  const addToast = useCallback((message: string, type: Toast['type'] = 'info') => {
    const id = Math.random().toString(36).slice(2);
    setToasts(prev => [...prev, { id, message, type }]);
    setTimeout(() => {
      setToasts(prev => prev.filter(t => t.id !== id));
    }, 4000);
  }, []);

  const removeToast = useCallback((id: string) => {
    setToasts(prev => prev.filter(t => t.id !== id));
  }, []);

  return { toasts, addToast, removeToast };
}
'''

print(f"Generated {len(files)} core files")
for name in files:
    print(f"  - {name}")



# Generate UI Components

# 9. src/components/ui/button.tsx
files["src/components/ui/button.tsx"] = '''import { ButtonHTMLAttributes, forwardRef } from 'react';
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'danger' | 'ghost' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  isLoading?: boolean;
}

const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant = 'primary', size = 'md', isLoading, children, disabled, ...props }, ref) => {
    const variants = {
      primary: 'bg-saffron-500 text-white hover:bg-saffron-600 active:bg-saffron-700 active:scale-[0.98]',
      secondary: 'bg-charcoal text-white hover:bg-charcoal/90 active:scale-[0.98]',
      danger: 'bg-alert-red text-white hover:bg-red-700 active:scale-[0.98]',
      ghost: 'bg-transparent text-charcoal hover:bg-gray-100',
      outline: 'border-2 border-saffron-500 text-saffron-600 hover:bg-saffron-50',
    };

    const sizes = {
      sm: 'px-3 py-1.5 text-sm',
      md: 'px-4 py-2.5 text-sm',
      lg: 'px-6 py-3 text-base',
    };

    return (
      <button
        ref={ref}
        className={cn(
          'inline-flex items-center justify-center rounded-lg font-medium transition-all duration-150',
          'focus:outline-none focus:ring-2 focus:ring-saffron-400 focus:ring-offset-2',
          'disabled:opacity-50 disabled:cursor-not-allowed disabled:active:scale-100',
          variants[variant],
          sizes[size],
          className
        )}
        disabled={disabled || isLoading}
        {...props}
      >
        {isLoading && (
          <svg className="animate-spin -ml-1 mr-2 h-4 w-4 text-current" fill="none" viewBox="0 0 24 24">
            <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
            <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
          </svg>
        )}
        {children}
      </button>
    );
  }
);

Button.displayName = 'Button';

export { Button };
'''

# 10. src/components/ui/card.tsx
files["src/components/ui/card.tsx"] = '''import { HTMLAttributes, forwardRef } from 'react';
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

interface CardProps extends HTMLAttributes<HTMLDivElement> {
  accent?: 'saffron' | 'green' | 'red' | 'blue' | 'amber';
  clickable?: boolean;
}

const Card = forwardRef<HTMLDivElement, CardProps>(
  ({ className, accent, clickable, children, ...props }, ref) => {
    const accentStyles = {
      saffron: 'border-l-4 border-l-saffron-500 bg-gradient-to-br from-warm-cream to-white',
      green: 'border-l-4 border-l-deep-green-500 bg-gradient-to-br from-green-50 to-white',
      red: 'border-l-4 border-l-alert-red bg-gradient-to-br from-red-50 to-white animate-subtle-pulse',
      blue: 'border-l-4 border-l-blue-500 bg-gradient-to-br from-blue-50 to-white',
      amber: 'border-l-4 border-l-pending-amber bg-gradient-to-br from-amber-50 to-white',
    };

    return (
      <div
        ref={ref}
        className={cn(
          'rounded-xl p-5 shadow-sm border border-gray-100',
          accent && accentStyles[accent],
          !accent && 'bg-white',
          clickable && 'cursor-pointer hover:shadow-md hover:-translate-y-0.5 transition-all duration-200',
          className
        )}
        {...props}
      >
        {children}
      </div>
    );
  }
);

Card.displayName = 'Card';

export { Card };
'''

# 11. src/components/ui/modal.tsx
files["src/components/ui/modal.tsx"] = '''"use client";

import { useEffect, useRef } from 'react';
import { createPortal } from 'react-dom';
import { X } from 'lucide-react';
import { Button } from './button';

interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title: string;
  children: React.ReactNode;
  footer?: React.ReactNode;
  size?: 'sm' | 'md' | 'lg';
}

export function Modal({ isOpen, onClose, title, children, footer, size = 'md' }: ModalProps) {
  const overlayRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden';
    } else {
      document.body.style.overflow = '';
    }
    return () => { document.body.style.overflow = ''; };
  }, [isOpen]);

  useEffect(() => {
    const handleEscape = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', handleEscape);
    return () => window.removeEventListener('keydown', handleEscape);
  }, [onClose]);

  if (!isOpen) return null;

  const sizes = {
    sm: 'max-w-md',
    md: 'max-w-lg',
    lg: 'max-w-2xl',
  };

  return createPortal(
    <div
      ref={overlayRef}
      className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50 backdrop-blur-sm"
      onClick={(e) => e.target === overlayRef.current && onClose()}
    >
      <div className={`${sizes[size]} w-full bg-white rounded-2xl shadow-2xl overflow-hidden`}>
        <div className="flex items-center justify-between px-6 py-4 border-b border-gray-100">
          <h3 className="text-lg font-semibold text-charcoal">{title}</h3>
          <button
            onClick={onClose}
            className="p-1 rounded-lg hover:bg-gray-100 transition-colors"
          >
            <X className="w-5 h-5 text-gray-500" />
          </button>
        </div>
        <div className="px-6 py-5">{children}</div>
        {footer && (
          <div className="flex items-center justify-end gap-3 px-6 py-4 bg-gray-50 border-t border-gray-100">
            {footer}
          </div>
        )}
      </div>
    </div>,
    document.body
  );
}
'''

# 12. src/components/ui/toast.tsx
files["src/components/ui/toast.tsx"] = '''"use client";

import { useEffect } from 'react';
import { CheckCircle, XCircle, AlertTriangle, Info, X } from 'lucide-react';
import { Toast as ToastType } from '@/hooks/use-toast';

interface ToastContainerProps {
  toasts: ToastType[];
  onRemove: (id: string) => void;
}

export function ToastContainer({ toasts, onRemove }: ToastContainerProps) {
  return (
    <div className="fixed top-4 right-4 z-50 flex flex-col gap-2">
      {toasts.map(toast => (
        <ToastItem key={toast.id} toast={toast} onRemove={onRemove} />
      ))}
    </div>
  );
}

function ToastItem({ toast, onRemove }: { toast: ToastType; onRemove: (id: string) => void }) {
  useEffect(() => {
    const timer = setTimeout(() => onRemove(toast.id), 4000);
    return () => clearTimeout(timer);
  }, [toast.id, onRemove]);

  const icons = {
    success: <CheckCircle className="w-5 h-5 text-green-600" />,
    error: <XCircle className="w-5 h-5 text-red-600" />,
    warning: <AlertTriangle className="w-5 h-5 text-amber-600" />,
    info: <Info className="w-5 h-5 text-blue-600" />,
  };

  const bgColors = {
    success: 'bg-green-50 border-green-200',
    error: 'bg-red-50 border-red-200',
    warning: 'bg-amber-50 border-amber-200',
    info: 'bg-blue-50 border-blue-200',
  };

  return (
    <div className={`flex items-start gap-3 px-4 py-3 rounded-xl border shadow-lg min-w-[320px] animate-in slide-in-from-right ${bgColors[toast.type]}`}>
      {icons[toast.type]}
      <p className="text-sm font-medium text-charcoal flex-1">{toast.message}</p>
      <button onClick={() => onRemove(toast.id)} className="p-0.5 hover:bg-black/5 rounded">
        <X className="w-4 h-4 text-gray-500" />
      </button>
    </div>
  );
}
'''

print(f"Generated {len(files)} UI component files")
for name in files:
    print(f"  - {name}")



# Generate Layout Components and Dashboard Components

# 13. src/components/layout/sidebar.tsx
files["src/components/layout/sidebar.tsx"] = '''"use client";

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useAuth } from '@/hooks/use-auth';
import {
  LayoutDashboard,
  Users,
  CreditCard,
  Home,
  MessageSquare,
  Droplets,
  UtensilsCrossed,
  UserCheck,
  LogOut,
  ChevronRight,
} from 'lucide-react';

const ownerNav = [
  { href: '/', label: 'Dashboard', icon: LayoutDashboard },
  { href: '/residents', label: 'Residents', icon: Users },
  { href: '/payments', label: 'Payments', icon: CreditCard },
  { href: '/rooms', label: 'Rooms', icon: Home },
  { href: '/complaints', label: 'Complaints', icon: MessageSquare },
  { href: '/iot', label: 'Utilities', icon: Droplets },
  { href: '/food', label: 'Food', icon: UtensilsCrossed },
  { href: '/staff', label: 'Staff', icon: UserCheck },
];

const staffNav = [
  { href: '/', label: 'Dashboard', icon: LayoutDashboard },
  { href: '/complaints', label: 'Tickets', icon: MessageSquare },
  { href: '/staff', label: 'My Tasks', icon: UserCheck },
];

export function Sidebar() {
  const pathname = usePathname();
  const { user, role, logout } = useAuth();

  const navItems = role === 'staff' ? staffNav : ownerNav;

  return (
    <aside className="hidden lg:flex flex-col w-64 h-screen bg-white border-r border-gray-100 fixed left-0 top-0">
      {/* Logo */}
      <div className="px-6 py-5 border-b border-gray-100">
        <Link href="/" className="flex items-center gap-2">
          <div className="w-8 h-8 bg-saffron-500 rounded-lg flex items-center justify-center">
            <span className="text-white font-bold text-sm">O</span>
          </div>
          <span className="text-xl font-display font-bold text-charcoal">Opsora</span>
        </Link>
      </div>

      {/* Navigation */}
      <nav className="flex-1 px-3 py-4 space-y-1 overflow-y-auto">
        {navItems.map((item) => {
          const isActive = pathname === item.href || pathname.startsWith(item.href + '/');
          const Icon = item.icon;
          return (
            <Link
              key={item.href}
              href={item.href}
              className={`flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-150 ${
                isActive
                  ? 'bg-saffron-50 text-saffron-700 border-l-3 border-l-saffron-500'
                  : 'text-gray-600 hover:bg-gray-50 hover:text-charcoal'
              }`}
            >
              <Icon className={`w-5 h-5 ${isActive ? 'text-saffron-500' : 'text-gray-400'}`} />
              <span>{item.label}</span>
              {isActive && <ChevronRight className="w-4 h-4 ml-auto text-saffron-400" />}
            </Link>
          );
        })}
      </nav>

      {/* User Profile */}
      <div className="p-4 border-t border-gray-100">
        <div className="flex items-center gap-3 mb-3">
          <div className="w-10 h-10 bg-gradient-to-br from-saffron-400 to-saffron-600 rounded-full flex items-center justify-center text-white font-semibold text-sm">
            {user?.fullName?.charAt(0) || 'A'}
          </div>
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium text-charcoal truncate">{user?.fullName || 'Admin'}</p>
            <p className="text-xs text-gray-500 capitalize">{role}</p>
          </div>
        </div>
        <button
          onClick={logout}
          className="flex items-center gap-2 w-full px-3 py-2 text-sm text-gray-600 hover:text-alert-red hover:bg-red-50 rounded-lg transition-colors"
        >
          <LogOut className="w-4 h-4" />
          Sign Out
        </button>
      </div>
    </aside>
  );
}
'''

# 14. src/components/layout/mobile-nav.tsx
files["src/components/layout/mobile-nav.tsx"] = '''"use client";

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useAuth } from '@/hooks/use-auth';
import {
  LayoutDashboard,
  Users,
  CreditCard,
  Home,
  MessageSquare,
  Plus,
} from 'lucide-react';

const navItems = [
  { href: '/', label: 'Home', icon: LayoutDashboard },
  { href: '/residents', label: 'Residents', icon: Users },
  { href: '/payments', label: 'Pay', icon: CreditCard },
  { href: '/complaints', label: 'Issues', icon: MessageSquare },
  { href: '/rooms', label: 'Rooms', icon: Home },
];

export function MobileNav() {
  const pathname = usePathname();
  const { role } = useAuth();

  if (role === 'resident') return null;

  return (
    <nav className="lg:hidden fixed bottom-0 left-0 right-0 bg-white border-t border-gray-100 z-40 pb-safe">
      <div className="flex items-center justify-around px-2 py-2">
        {navItems.map((item) => {
          const isActive = pathname === item.href;
          const Icon = item.icon;
          return (
            <Link
              key={item.href}
              href={item.href}
              className={`flex flex-col items-center gap-1 px-3 py-1.5 rounded-xl min-w-[64px] ${
                isActive ? 'text-saffron-600' : 'text-gray-400'
              }`}
            >
              <Icon className={`w-5 h-5 ${isActive ? 'text-saffron-500' : ''}`} />
              <span className="text-[10px] font-medium">{item.label}</span>
            </Link>
          );
        })}
        <Link
          href="/residents/new"
          className="flex flex-col items-center gap-1 px-3 py-1.5 rounded-xl min-w-[64px] text-saffron-600"
        >
          <div className="w-8 h-8 bg-saffron-500 rounded-full flex items-center justify-center">
            <Plus className="w-4 h-4 text-white" />
          </div>
          <span className="text-[10px] font-medium">Add</span>
        </Link>
      </div>
    </nav>
  );
}
'''

# 15. src/components/dashboard/revenue-card.tsx
files["src/components/dashboard/revenue-card.tsx"] = '''"use client";

import Link from 'next/link';
import { TrendingUp, TrendingDown, ArrowRight } from 'lucide-react';
import { Card } from '@/components/ui/card';

interface RevenueCardProps {
  title: string;
  amount: number;
  target?: number;
  trend?: string;
  trendUp?: boolean;
  href: string;
  accent?: 'saffron' | 'green' | 'red';
}

export function RevenueCard({ title, amount, target, trend, trendUp, href, accent = 'saffron' }: RevenueCardProps) {
  const formatCurrency = (val: number) => {
    if (val >= 100000) return `₹${(val / 100000).toFixed(1)}L`;
    if (val >= 1000) return `₹${(val / 1000).toFixed(1)}K`;
    return `₹${val}`;
  };

  const progress = target && target > 0 ? (amount / target) * 100 : 0;

  return (
    <Card accent={accent} clickable>
      <Link href={href} className="block">
        <div className="flex items-start justify-between mb-3">
          <div>
            <p className="text-sm text-gray-500 font-medium">{title}</p>
            <p className="text-2xl font-bold text-charcoal mt-1 tracking-tight">
              {formatCurrency(amount)}
            </p>
          </div>
          {trend && (
            <div className={`flex items-center gap-1 px-2 py-1 rounded-full text-xs font-medium ${
              trendUp ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700'
            }`}>
              {trendUp ? <TrendingUp className="w-3 h-3" /> : <TrendingDown className="w-3 h-3" />}
              {trend}
            </div>
          )}
        </div>
        
        {target && (
          <div className="mt-3">
            <div className="flex items-center justify-between text-xs text-gray-500 mb-1.5">
              <span>{Math.round(progress)}% of target</span>
              <span>{formatCurrency(target)}</span>
            </div>
            <div className="h-2 bg-gray-100 rounded-full overflow-hidden">
              <div
                className={`h-full rounded-full transition-all duration-500 ${
                  progress >= 80 ? 'bg-deep-green-500' : progress >= 50 ? 'bg-saffron-500' : 'bg-pending-amber'
                }`}
                style={{ width: `${Math.min(progress, 100)}%` }}
              />
            </div>
          </div>
        )}
        
        <div className="flex items-center gap-1 mt-3 text-sm text-saffron-600 font-medium">
          View Details
          <ArrowRight className="w-4 h-4" />
        </div>
      </Link>
    </Card>
  );
}
'''

# 16. src/components/dashboard/occupancy-card.tsx
files["src/components/dashboard/occupancy-card.tsx"] = '''"use client";

import Link from 'next/link';
import { Bed, ArrowRight } from 'lucide-react';
import { Card } from '@/components/ui/card';

interface OccupancyCardProps {
  occupied: number;
  total: number;
  href: string;
}

export function OccupancyCard({ occupied, total, href }: OccupancyCardProps) {
  const percentage = total > 0 ? Math.round((occupied / total) * 100) : 0;
  const vacant = total - occupied;

  return (
    <Card accent="green" clickable>
      <Link href={href} className="block">
        <div className="flex items-start justify-between mb-3">
          <div>
            <p className="text-sm text-gray-500 font-medium">Occupancy Rate</p>
            <p className="text-2xl font-bold text-charcoal mt-1 tracking-tight">
              {percentage}%
            </p>
          </div>
          <div className="w-10 h-10 bg-green-100 rounded-xl flex items-center justify-center">
            <Bed className="w-5 h-5 text-deep-green-500" />
          </div>
        </div>
        
        <div className="flex items-center gap-4 mt-3">
          <div className="flex-1">
            <p className="text-xs text-gray-500">Occupied</p>
            <p className="text-lg font-semibold text-charcoal">{occupied}</p>
          </div>
          <div className="w-px h-8 bg-gray-200" />
          <div className="flex-1">
            <p className="text-xs text-gray-500">Vacant</p>
            <p className="text-lg font-semibold text-saffron-600">{vacant}</p>
          </div>
          <div className="w-px h-8 bg-gray-200" />
          <div className="flex-1">
            <p className="text-xs text-gray-500">Total</p>
            <p className="text-lg font-semibold text-charcoal">{total}</p>
          </div>
        </div>
        
        <div className="flex items-center gap-1 mt-3 text-sm text-deep-green-600 font-medium">
          View Floor Map
          <ArrowRight className="w-4 h-4" />
        </div>
      </Link>
    </Card>
  );
}
'''

# 17. src/components/dashboard/complaint-card.tsx
files["src/components/dashboard/complaint-card.tsx"] = '''"use client";

import Link from 'next/link';
import { AlertCircle, ArrowRight, Clock } from 'lucide-react';
import { Card } from '@/components/ui/card';

interface ComplaintCardProps {
  open: number;
  urgent: number;
  href: string;
}

export function ComplaintCard({ open, urgent, href }: ComplaintCardProps) {
  return (
    <Card accent={urgent > 0 ? 'red' : 'amber'} clickable>
      <Link href={href} className="block">
        <div className="flex items-start justify-between mb-3">
          <div>
            <p className="text-sm text-gray-500 font-medium">Open Complaints</p>
            <p className="text-2xl font-bold text-charcoal mt-1 tracking-tight">
              {open}
            </p>
          </div>
          <div className={`w-10 h-10 rounded-xl flex items-center justify-center ${
            urgent > 0 ? 'bg-red-100' : 'bg-amber-100'
          }`}>
            <AlertCircle className={`w-5 h-5 ${urgent > 0 ? 'text-alert-red' : 'text-pending-amber'}`} />
          </div>
        </div>
        
        {urgent > 0 && (
          <div className="flex items-center gap-2 mt-2 px-3 py-2 bg-red-50 rounded-lg">
            <Clock className="w-4 h-4 text-alert-red" />
            <span className="text-sm font-medium text-alert-red">
              {urgent} urgent need attention
            </span>
          </div>
        )}
        
        <div className="flex items-center gap-1 mt-3 text-sm text-alert-red font-medium">
          View All Tickets
          <ArrowRight className="w-4 h-4" />
        </div>
      </Link>
    </Card>
  );
}
'''

print(f"Generated {len(files)} layout & dashboard files")
for name in files:
    print(f"  - {name}")



# Generate Warning Modal Components

# 18. src/components/modals/checkout-warning.tsx
files["src/components/modals/checkout-warning.tsx"] = '''"use client";

import { useState } from 'react';
import { AlertTriangle, Receipt, Wallet, MessageSquare } from 'lucide-react';
import { Modal } from '@/components/ui/modal';
import { Button } from '@/components/ui/button';

interface CheckoutWarningProps {
  isOpen: boolean;
  onClose: () => void;
  onConfirm: () => void;
  resident: {
    id: string;
    fullName: string;
    roomNumber: string;
    pendingAmount: number;
    depositPaid: number;
    openComplaints: Array<{ id: string; title: string }>;
  } | null;
  isLoading?: boolean;
}

export function CheckoutWarningModal({
  isOpen,
  onClose,
  onConfirm,
  resident,
  isLoading,
}: CheckoutWarningProps) {
  const [confirmText, setConfirmText] = useState('');
  
  if (!resident) return null;
  
  const refundAmount = resident.depositPaid - resident.pendingAmount;
  const hasIssues = resident.pendingAmount > 0 || resident.openComplaints.length > 0;

  return (
    <Modal
      isOpen={isOpen}
      onClose={onClose}
      title="⚠️ Confirm Resident Checkout"
      size="lg"
      footer={
        <>
          <Button variant="ghost" onClick={onClose} disabled={isLoading}>
            Cancel
          </Button>
          <Button
            variant="danger"
            onClick={onConfirm}
            isLoading={isLoading}
            disabled={hasIssues && confirmText !== 'CHECKOUT'}
          >
            Confirm Checkout
          </Button>
        </>
      }
    >
      <div className="space-y-5">
        {/* Resident Info */}
        <div className="flex items-center gap-3 p-3 bg-gray-50 rounded-xl">
          <div className="w-10 h-10 bg-saffron-100 rounded-full flex items-center justify-center text-saffron-600 font-bold">
            {resident.fullName.charAt(0)}
          </div>
          <div>
            <p className="font-semibold text-charcoal">{resident.fullName}</p>
            <p className="text-sm text-gray-500">Room {resident.roomNumber}</p>
          </div>
        </div>

        {/* Financial Summary */}
        <div className="border border-gray-200 rounded-xl overflow-hidden">
          <div className="px-4 py-3 bg-gray-50 border-b border-gray-200 flex items-center gap-2">
            <Wallet className="w-4 h-4 text-gray-500" />
            <span className="text-sm font-semibold text-charcoal">Financial Summary</span>
          </div>
          <div className="p-4 space-y-2">
            <div className="flex justify-between text-sm">
              <span className="text-gray-600">Total Rent Due</span>
              <span className="font-medium text-charcoal">₹{resident.pendingAmount.toLocaleString()}</span>
            </div>
            <div className="flex justify-between text-sm">
              <span className="text-gray-600">Deposit Paid</span>
              <span className="font-medium text-charcoal">₹{resident.depositPaid.toLocaleString()}</span>
            </div>
            <div className="h-px bg-gray-200 my-2" />
            <div className="flex justify-between">
              <span className="font-semibold text-charcoal">Refund Amount</span>
              <span className={`font-bold text-lg ${refundAmount >= 0 ? 'text-deep-green-500' : 'text-alert-red'}`}>
                ₹{Math.abs(refundAmount).toLocaleString()}
                {refundAmount < 0 && ' (Additional Due)'}
              </span>
            </div>
          </div>
        </div>

        {/* Open Complaints */}
        {resident.openComplaints.length > 0 && (
          <div className="border border-red-200 rounded-xl overflow-hidden">
            <div className="px-4 py-3 bg-red-50 border-b border-red-200 flex items-center gap-2">
              <MessageSquare className="w-4 h-4 text-alert-red" />
              <span className="text-sm font-semibold text-alert-red">
                Open Complaints ({resident.openComplaints.length})
              </span>
            </div>
            <div className="p-4 space-y-2">
              {resident.openComplaints.map((c) => (
                <div key={c.id} className="flex items-start gap-2 text-sm">
                  <AlertTriangle className="w-4 h-4 text-amber-500 mt-0.5 shrink-0" />
                  <span className="text-gray-700">{c.title}</span>
                </div>
              ))}
              <p className="text-xs text-gray-500 mt-2">
                These will be auto-closed on checkout. Consider resolving them first.
              </p>
            </div>
          </div>
        )}

        {/* Warnings */}
        {hasIssues && (
          <div className="p-4 bg-amber-50 border border-amber-200 rounded-xl">
            <div className="flex items-start gap-2">
              <AlertTriangle className="w-5 h-5 text-pending-amber shrink-0 mt-0.5" />
              <div>
                <p className="text-sm font-semibold text-pending-amber">Action Required</p>
                <p className="text-sm text-gray-700 mt-1">
                  This resident has pending dues or open complaints. Type <strong>CHECKOUT</strong> to confirm.
                </p>
                <input
                  type="text"
                  value={confirmText}
                  onChange={(e) => setConfirmText(e.target.value)}
                  placeholder="Type CHECKOUT to confirm"
                  className="mt-3 w-full px-3 py-2 border border-amber-300 rounded-lg text-sm focus:ring-2 focus:ring-amber-400 focus:border-amber-400"
                />
              </div>
            </div>
          </div>
        )}

        {/* Receipt Note */}
        <div className="flex items-center gap-2 text-xs text-gray-500">
          <Receipt className="w-4 h-4" />
          <span>A checkout receipt will be automatically generated</span>
        </div>
      </div>
    </Modal>
  );
}
'''

# 19. src/components/modals/payment-verify-warning.tsx
files["src/components/modals/payment-verify-warning.tsx"] = '''"use client";

import { useState } from 'react';
import { AlertTriangle, Receipt, ImageIcon, MessageSquare } from 'lucide-react';
import { Modal } from '@/components/ui/modal';
import { Button } from '@/components/ui/button';

interface PaymentVerifyWarningProps {
  isOpen: boolean;
  onClose: () => void;
  onVerify: (action: 'approve' | 'reject' | 'reupload', reason?: string) => void;
  proof: {
    id: string;
    residentName: string;
    roomNumber: string;
    amountPaid: number;
    invoiceAmount: number;
    invoiceNumber: string;
    screenshotUrl: string;
    paymentDate: string;
    transactionReference?: string;
  } | null;
  isLoading?: boolean;
}

export function PaymentVerifyWarningModal({
  isOpen,
  onClose,
  onVerify,
  proof,
  isLoading,
}: PaymentVerifyWarningProps) {
  const [action, setAction] = useState<'approve' | 'reject' | 'reupload' | null>(null);
  const [reason, setReason] = useState('');
  
  if (!proof) return null;
  
  const isOverpaid = proof.amountPaid > proof.invoiceAmount;
  const isUnderpaid = proof.amountPaid < proof.invoiceAmount;

  return (
    <Modal
      isOpen={isOpen}
      onClose={onClose}
      title="✅ Verify Payment"
      size="lg"
      footer={
        <>
          <Button variant="ghost" onClick={onClose} disabled={isLoading}>
            Cancel
          </Button>
          {action === 'approve' && (
            <Button
              variant="primary"
              onClick={() => onVerify('approve')}
              isLoading={isLoading}
            >
              Approve Payment
            </Button>
          )}
          {action === 'reject' && (
            <Button
              variant="danger"
              onClick={() => onVerify('reject', reason)}
              isLoading={isLoading}
              disabled={!reason.trim()}
            >
              Reject Payment
            </Button>
          )}
          {action === 'reupload' && (
            <Button
              variant="outline"
              onClick={() => onVerify('reupload', reason)}
              isLoading={isLoading}
            >
              Request Reupload
            </Button>
          )}
        </>
      }
    >
      <div className="space-y-5">
        {/* Resident & Invoice Info */}
        <div className="flex items-center justify-between p-3 bg-gray-50 rounded-xl">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center text-green-600 font-bold">
              {proof.residentName.charAt(0)}
            </div>
            <div>
              <p className="font-semibold text-charcoal">{proof.residentName}</p>
              <p className="text-sm text-gray-500">Room {proof.roomNumber}</p>
            </div>
          </div>
          <div className="text-right">
            <p className="text-xs text-gray-500">Invoice</p>
            <p className="text-sm font-medium text-charcoal">{proof.invoiceNumber}</p>
          </div>
        </div>

        {/* Amount Comparison */}
        <div className="grid grid-cols-2 gap-4">
          <div className="p-4 bg-gray-50 rounded-xl text-center">
            <p className="text-xs text-gray-500 mb-1">Invoice Amount</p>
            <p className="text-xl font-bold text-charcoal">₹{proof.invoiceAmount.toLocaleString()}</p>
          </div>
          <div className={`p-4 rounded-xl text-center ${
            isUnderpaid ? 'bg-red-50' : isOverpaid ? 'bg-blue-50' : 'bg-green-50'
          }`}>
            <p className="text-xs text-gray-500 mb-1">Amount Paid</p>
            <p className={`text-xl font-bold ${
              isUnderpaid ? 'text-alert-red' : isOverpaid ? 'text-blue-600' : 'text-deep-green-500'
            }`}>
              ₹{proof.amountPaid.toLocaleString()}
            </p>
          </div>
        </div>

        {/* Mismatch Warning */}
        {(isUnderpaid || isOverpaid) && (
          <div className="p-4 bg-amber-50 border border-amber-200 rounded-xl flex items-start gap-3">
            <AlertTriangle className="w-5 h-5 text-pending-amber shrink-0 mt-0.5" />
            <div>
              <p className="text-sm font-semibold text-pending-amber">
                Amount Mismatch Detected
              </p>
              <p className="text-sm text-gray-700 mt-1">
                {isUnderpaid 
                  ? `Short by ₹${(proof.invoiceAmount - proof.amountPaid).toLocaleString()}. Partial payment will be recorded.`
                  : `Overpaid by ₹${(proof.amountPaid - proof.invoiceAmount).toLocaleString()}. Excess will be recorded as advance.`
                }
              </p>
            </div>
          </div>
        )}

        {/* Screenshot Preview */}
        <div className="border border-gray-200 rounded-xl overflow-hidden">
          <div className="px-4 py-3 bg-gray-50 border-b border-gray-200 flex items-center gap-2">
            <ImageIcon className="w-4 h-4 text-gray-500" />
            <span className="text-sm font-semibold text-charcoal">Payment Screenshot</span>
          </div>
          <div className="p-4">
            {proof.screenshotUrl ? (
              <img 
                src={proof.screenshotUrl} 
                alt="Payment proof" 
                className="max-h-64 mx-auto rounded-lg border border-gray-200"
              />
            ) : (
              <div className="h-32 flex items-center justify-center text-gray-400 text-sm">
                No screenshot available
              </div>
            )}
            {proof.transactionReference && (
              <p className="text-xs text-gray-500 mt-2 text-center">
                Ref: {proof.transactionReference}
              </p>
            )}
          </div>
        </div>

        {/* Action Selection */}
        <div className="space-y-2">
          <p className="text-sm font-semibold text-charcoal">Choose Action:</p>
          <div className="grid grid-cols-3 gap-2">
            <button
              onClick={() => setAction('approve')}
              className={`p-3 rounded-xl border-2 text-sm font-medium transition-all ${
                action === 'approve'
                  ? 'border-green-500 bg-green-50 text-green-700'
                  : 'border-gray-200 hover:border-green-300 text-gray-600'
              }`}
            >
              ✅ Approve
            </button>
            <button
              onClick={() => setAction('reject')}
              className={`p-3 rounded-xl border-2 text-sm font-medium transition-all ${
                action === 'reject'
                  ? 'border-red-500 bg-red-50 text-red-700'
                  : 'border-gray-200 hover:border-red-300 text-gray-600'
              }`}
            >
              ❌ Reject
            </button>
            <button
              onClick={() => setAction('reupload')}
              className={`p-3 rounded-xl border-2 text-sm font-medium transition-all ${
                action === 'reupload'
                  ? 'border-amber-500 bg-amber-50 text-amber-700'
                  : 'border-gray-200 hover:border-amber-300 text-gray-600'
              }`}
            >
              📝 Reupload
            </button>
          </div>
        </div>

        {/* Reason Input */}
        {(action === 'reject' || action === 'reupload') && (
          <div className="space-y-2">
            <label className="text-sm font-medium text-charcoal">
              {action === 'reject' ? 'Rejection Reason *' : 'Request Notes'}
            </label>
            <textarea
              value={reason}
              onChange={(e) => setReason(e.target.value)}
              placeholder={action === 'reject' ? 'Why is this payment being rejected?' : 'What should the resident fix?'}
              className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-saffron-400 focus:border-saffron-400 min-h-[80px] resize-none"
            />
          </div>
        )}

        {/* Info Note */}
        <div className="flex items-start gap-2 text-xs text-gray-500">
          <Receipt className="w-4 h-4 shrink-0 mt-0.5" />
          <span>
            Approving will auto-generate a receipt and notify the resident. 
            This action cannot be undone.
          </span>
        </div>
      </div>
    </Modal>
  );
}
'''

# 20. src/components/modals/bulk-invoice-warning.tsx
files["src/components/modals/bulk-invoice-warning.tsx"] = '''"use client";

import { useState } from 'react';
import { AlertTriangle, FileText, Users, Calendar, IndianRupee } from 'lucide-react';
import { Modal } from '@/components/ui/modal';
import { Button } from '@/components/ui/button';

interface BulkInvoiceWarningProps {
  isOpen: boolean;
  onClose: () => void;
  onConfirm: () => void;
  data: {
    monthYear: string;
    dueDate: string;
    affectedResidents: number;
    alreadyInvoiced: number;
    newInvoices: number;
    totalValue: number;
  } | null;
  isLoading?: boolean;
}

export function BulkInvoiceWarningModal({
  isOpen,
  onClose,
  onConfirm,
  data,
  isLoading,
}: BulkInvoiceWarningProps) {
  const [confirmText, setConfirmText] = useState('');
  
  if (!data) return null;

  return (
    <Modal
      isOpen={isOpen}
      onClose={onClose}
      title="📋 Generate Monthly Invoices"
      size="md"
      footer={
        <>
          <Button variant="ghost" onClick={onClose} disabled={isLoading}>
            Cancel
          </Button>
          <Button
            variant="primary"
            onClick={onConfirm}
            isLoading={isLoading}
            disabled={confirmText !== 'GENERATE'}
          >
            Generate {data.newInvoices} Invoices
          </Button>
        </>
      }
    >
      <div className="space-y-5">
        {/* Month & Due Date */}
        <div className="grid grid-cols-2 gap-4">
          <div className="flex items-center gap-3 p-3 bg-gray-50 rounded-xl">
            <Calendar className="w-5 h-5 text-saffron-500" />
            <div>
              <p className="text-xs text-gray-500">Month</p>
              <p className="font-semibold text-charcoal">{data.monthYear}</p>
            </div>
          </div>
          <div className="flex items-center gap-3 p-3 bg-gray-50 rounded-xl">
            <Calendar className="w-5 h-5 text-saffron-500" />
            <div>
              <p className="text-xs text-gray-500">Due Date</p>
              <p className="font-semibold text-charcoal">{data.dueDate}</p>
            </div>
          </div>
        </div>

        {/* Impact Summary */}
        <div className="border border-gray-200 rounded-xl overflow-hidden">
          <div className="px-4 py-3 bg-gray-50 border-b border-gray-200 flex items-center gap-2">
            <Users className="w-4 h-4 text-gray-500" />
            <span className="text-sm font-semibold text-charcoal">Impact Summary</span>
          </div>
          <div className="p-4 space-y-3">
            <div className="flex justify-between items-center">
              <span className="text-sm text-gray-600">Active Residents</span>
              <span className="font-medium text-charcoal">{data.affectedResidents}</span>
            </div>
            <div className="flex justify-between items-center">
              <span className="text-sm text-gray-600">Already Invoiced</span>
              <span className="font-medium text-gray-500">{data.alreadyInvoiced} (will be skipped)</span>
            </div>
            <div className="flex justify-between items-center">
              <span className="text-sm text-gray-600">New Invoices</span>
              <span className="font-bold text-saffron-600">{data.newInvoices}</span>
            </div>
            <div className="h-px bg-gray-200" />
            <div className="flex justify-between items-center">
              <span className="text-sm font-semibold text-charcoal">Total Invoice Value</span>
              <span className="font-bold text-lg text-deep-green-500">
                ₹{data.totalValue.toLocaleString()}
              </span>
            </div>
          </div>
        </div>

        {/* Warning */}
        <div className="p-4 bg-amber-50 border border-amber-200 rounded-xl">
          <div className="flex items-start gap-3">
            <AlertTriangle className="w-5 h-5 text-pending-amber shrink-0 mt-0.5" />
            <div>
              <p className="text-sm font-semibold text-pending-amber">Important</p>
              <ul className="text-sm text-gray-700 mt-1 space-y-1">
                <li>• This will create {data.newInvoices} new invoices</li>
                <li>• Existing invoices for this month will NOT be overwritten</li>
                <li>• Residents will be notified automatically</li>
                <li>• This action cannot be undone</li>
              </ul>
              <p className="text-sm text-gray-700 mt-3">
                Type <strong>GENERATE</strong> to confirm:
              </p>
              <input
                type="text"
                value={confirmText}
                onChange={(e) => setConfirmText(e.target.value)}
                placeholder="Type GENERATE to confirm"
                className="mt-2 w-full px-3 py-2 border border-amber-300 rounded-lg text-sm focus:ring-2 focus:ring-amber-400 focus:border-amber-400"
              />
            </div>
          </div>
        </div>
      </div>
    </Modal>
  );
}
'''

# 21. src/components/modals/delete-warning.tsx
files["src/components/modals/delete-warning.tsx"] = '''"use client";

import { useState } from 'react';
import { AlertTriangle, Trash2, Users, MessageSquare, CreditCard, Building } from 'lucide-react';
import { Modal } from '@/components/ui/modal';
import { Button } from '@/components/ui/button';

interface DeleteWarningProps {
  isOpen: boolean;
  onClose: () => void;
  onConfirm: () => void;
  entity: {
    type: 'property' | 'resident' | 'room' | 'user';
    name: string;
    impact: {
      activeResidents?: number;
      openComplaints?: number;
      pendingPayments?: number;
      staffCount?: number;
      historicalData?: string;
    };
  } | null;
  isLoading?: boolean;
}

export function DeleteWarningModal({
  isOpen,
  onClose,
  onConfirm,
  entity,
  isLoading,
}: DeleteWarningProps) {
  const [confirmText, setConfirmText] = useState('');
  
  if (!entity) return null;

  const entityLabels = {
    property: 'Property',
    resident: 'Resident',
    room: 'Room',
    user: 'User',
  };

  const entityIcons = {
    property: Building,
    resident: Users,
    room: Building,
    user: Users,
  };

  const Icon = entityIcons[entity.type];

  return (
    <Modal
      isOpen={isOpen}
      onClose={onClose}
      title={`🗑️ Delete ${entityLabels[entity.type]}`}
      size="md"
      footer={
        <>
          <Button variant="ghost" onClick={onClose} disabled={isLoading}>
            Cancel
          </Button>
          <Button
            variant="danger"
            onClick={onConfirm}
            isLoading={isLoading}
            disabled={confirmText !== 'DELETE'}
          >
            <Trash2 className="w-4 h-4 mr-2" />
            Delete Forever
          </Button>
        </>
      }
    >
      <div className="space-y-5">
        {/* Entity Info */}
        <div className="flex items-center gap-3 p-3 bg-red-50 rounded-xl">
          <div className="w-10 h-10 bg-red-100 rounded-full flex items-center justify-center">
            <Icon className="w-5 h-5 text-alert-red" />
          </div>
          <div>
            <p className="font-semibold text-charcoal">{entity.name}</p>
            <p className="text-sm text-gray-500">{entityLabels[entity.type]}</p>
          </div>
        </div>

        {/* Impact Analysis */}
        <div className="border border-red-200 rounded-xl overflow-hidden">
          <div className="px-4 py-3 bg-red-50 border-b border-red-200 flex items-center gap-2">
            <AlertTriangle className="w-4 h-4 text-alert-red" />
            <span className="text-sm font-semibold text-alert-red">Impact Analysis</span>
          </div>
          <div className="p-4 space-y-3">
            {entity.impact.activeResidents !== undefined && entity.impact.activeResidents > 0 && (
              <div className="flex items-center gap-3 text-sm">
                <Users className="w-4 h-4 text-gray-500" />
                <span className="text-gray-600">Active Residents:</span>
                <span className="font-medium text-charcoal">{entity.impact.activeResidents}</span>
              </div>
            )}
            {entity.impact.openComplaints !== undefined && entity.impact.openComplaints > 0 && (
              <div className="flex items-center gap-3 text-sm">
                <MessageSquare className="w-4 h-4 text-gray-500" />
                <span className="text-gray-600">Open Complaints:</span>
                <span className="font-medium text-charcoal">{entity.impact.openComplaints}</span>
              </div>
            )}
            {entity.impact.pendingPayments !== undefined && entity.impact.pendingPayments > 0 && (
              <div className="flex items-center gap-3 text-sm">
                <CreditCard className="w-4 h-4 text-gray-500" />
                <span className="text-gray-600">Pending Payments:</span>
                <span className="font-medium text-charcoal">₹{entity.impact.pendingPayments.toLocaleString()}</span>
              </div>
            )}
            {entity.impact.staffCount !== undefined && entity.impact.staffCount > 0 && (
              <div className="flex items-center gap-3 text-sm">
                <Users className="w-4 h-4 text-gray-500" />
                <span className="text-gray-600">Staff Assigned:</span>
                <span className="font-medium text-charcoal">{entity.impact.staffCount}</span>
              </div>
            )}
            {entity.impact.historicalData && (
              <div className="flex items-center gap-3 text-sm">
                <Building className="w-4 h-4 text-gray-500" />
                <span className="text-gray-600">Historical Data:</span>
                <span className="font-medium text-charcoal">{entity.impact.historicalData}</span>
              </div>
            )}
          </div>
        </div>

        {/* Permanent Warning */}
        <div className="p-4 bg-red-50 border border-red-200 rounded-xl">
          <div className="flex items-start gap-3">
            <AlertTriangle className="w-5 h-5 text-alert-red shrink-0 mt-0.5" />
            <div>
              <p className="text-sm font-semibold text-alert-red">This action is permanent</p>
              <p className="text-sm text-gray-700 mt-1">
                All associated data will be permanently deleted and cannot be recovered. 
                Type <strong>DELETE</strong> to confirm.
              </p>
              <input
                type="text"
                value={confirmText}
                onChange={(e) => setConfirmText(e.target.value)}
                placeholder="Type DELETE to confirm"
                className="mt-3 w-full px-3 py-2 border border-red-300 rounded-lg text-sm focus:ring-2 focus:ring-red-400 focus:border-red-400"
              />
            </div>
          </div>
        </div>
      </div>
    </Modal>
  );
}
'''

print(f"Generated {len(files)} warning modal files")
for name in files:
    print(f"  - {name}")
